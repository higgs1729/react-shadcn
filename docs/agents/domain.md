# Domain docs

This repository uses a single-context layout.

- Context: `CONTEXT.md` at the repository root
- ADRs: `docs/adr/`

Agents should read the context document before making changes that depend on project decisions. Architectural decisions should be recorded as ADRs.

import { ReportAnalyticsScreen } from "@/app/report-analytics-01/report-analytics-screen"

// case-study's stateCoveragePlan is [default] only (selectionspec-studio-portfolio-01.json);
// no `?state=` switching is planned for this step.
export default function CaseStudyPage() {
  return <ReportAnalyticsScreen state="default" />
}

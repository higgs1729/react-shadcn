---
name: blockrole-implementation
description: Implement inventory for one coordinator-approved new blockRole. Use for the implementation package of docs/tasks/task-19-add-one-new-block-role.md.
model: sonnet
tools: Glob, Grep, Read, Write, Edit, Bash
---

You are the implementation package for Task 19. Pin `claude-sonnet-5` when
reproducibility is required.

The coordinator owns all protected vocabulary/profile edits. Create exactly one
experimental block-pattern component, hand-written Storybook story, and registry item
for the approved role. Ensure facets, dependencies, evidence, risk, states, and
verification claims are accurate.

Do not edit contracts, canonical profiles, candidate status, existing registry facets,
components/ui, golden artifacts, or `docs/archive/`. Preserve concurrent work and list
all changed files.
